# POS 上线 · Plan 3：web-admin 加 POS 入口 + 桌面主流程验收 + 整体上线 - 实施计划

> 依据设计：`specs/2026-09-20-pos-entry-design.md`
> 定稿方向：桌面 POS 由 `pos-app` 承载，部署于同域 `/guanli/pos/`；web-admin（uni-app 移动后台）仅加收银入口跳转，验收为收银/结账/聚合收款/交班/退款/促销/报表桌面回归。

- [ ] Task 1：web-admin 新增「门店收银 POS」入口（菜单项/工作台卡片，`pages/dashboard/index.vue` 或等效导航处），`window.location.href` 跳同域 `/guanli/pos/`（动态 origin，禁硬编码域名）；`.env*` 配置 POS 路径。
- [ ] Task 2：i18n 同步（zh-Hans/en 各加入口文案），`pnpm build:h5` 通过。
- [ ] Task 3：共享后端下回归收银主流程：Cashier 加购建单 → Checkout 结账 → 支付/聚合收款 → Shift 交班对账；每步 API 正常、订单/收款台账落库与实收一致。
- [ ] Task 4：回归 Refund 退款、Promotion 促销（满减/折扣/买赠）、Report 报表（今日/销售额/月度/热销 top）；确认 channel 切换后数据随 activeChannel 变化。
- [ ] Task 5：截图：web-admin 入口（移动视口）+ 桌面 POS 各主流程（电脑版视口），补操作手册；git add + commit。
- [ ] Task 6：整体上线：本地构建 `pos-app` 产物 scp 到站点 `guanli/pos/`（解压/拷入，不服务器构建）；web-admin 走 `scripts/deploy.mjs`；vendure 后端 git pull + pm2 restart。