# POS 上线 · Plan 3：web-admin 加 POS 收银入口 + 主流程验收 - 实施计划

> 依据设计：`specs/2026-09-20-pos-entry-design.md`

- [ ] Task 1：web-admin 新增「门店收银 POS」入口（菜单项/工作台卡片，`dashboard/index.vue` 或等效导航处），`window.location.href` 跳同域桌面 POS 路径；`.env*` 配置 POS 路径（动态 origin，禁硬编码域名）。
- [ ] Task 2：i18n 同步（zh-Hans/en 各加菜单文案），`pnpm build:h5` 通过。
- [ ] Task 3：共享后端数据下回归收银主流程：Cashier 收银建单 → Checkout 结账 → 支付/聚合收款 → Shift 交班对账；每步 API 正常、订单/台账落库与实收一致。
- [ ] Task 4：回归 Refund 退款、Promotion 促销（满减/折扣/买赠）、Report 报表（todayOverview/sales/monthly/topProducts）；确认 channel 切换后报表随 activeChannel 变化。
- [ ] Task 5：截图：web-admin 入口（移动视口）+ 桌面 POS 各主流程（电脑版视口），补操作手册；git add + commit（web-admin 仓库）。
- [ ] Task 6：整体上线：本地构建产物 scp 到服务器，解压/拷入（web-admin 走 deploy.mjs；vendure 后端 git pull + pm2 restart），不在服务器构建。