# VShop 前端使用手册

## 1. 环境准备

### 1.1 系统要求

| 组件 | 版本 | 说明 |
|------|------|------|
| Node.js | >= 20.x | LTS 版本 |
| npm | >= 9 | 包管理器 |

### 1.2 安装依赖

```bash
cd e:/code/vshop
npm install
```

---

## 2. 启动与运行

### 2.1 H5 开发

```bash
npm run dev:h5
```

启动后访问 http://localhost:5173（端口可能自动递增）。

### 2.2 微信小程序开发

```bash
npm run dev:mp-weixin
```

编译输出到 `dist/dev/mp-weixin/` 目录，使用**微信开发者工具**打开该目录。

### 2.3 构建生产版本

```bash
# H5 生产构建
npm run build:h5
# 输出到 dist/build/h5/

# 小程序生产构建
npm run build:mp-weixin
# 输出到 dist/build/mp-weixin/
```

---

## 3. 环境变量

### 3.1 配置文件

| 文件 | 用途 |
|------|------|
| `.env` | 所有环境通用 |
| `.env.development` | 开发环境 |
| `.env.production` | 生产环境 |

### 3.2 变量清单

| 变量 | 说明 | 开发默认值 | 生产示例 |
|------|------|-----------|---------|
| VITE_API_URL | 后端 API 地址 | http://localhost:3000 | https://api.yourshop.com |
| VITE_APP_TITLE | 应用标题 | VShop Dev | VShop |
| VITE_WECHAT_APP_ID | 微信公众号 AppID | （空） | wx1234567890abcdef |

### 3.3 使用方式

```typescript
const apiUrl = import.meta.env.VITE_API_URL;
const wechatAppId = import.meta.env.VITE_WECHAT_APP_ID || '';
```

---

## 4. 页面功能说明

### 4.1 主包页面

| 页面 | 路径 | 功能 | Tab |
|------|------|------|-----|
| 首页 | pages/home/index | 商品推荐、Banner、活动入口 | ✅ |
| 分类 | pages/category/index | 商品分类浏览 | ✅ |
| 购物车 | pages/cart/index | 购物车管理、结算入口 | ✅ |
| 登录 | pages/login/index | 多模式登录（微信/手机/本地） | - |
| 我的 | pages/profile/index | 个人中心、订单入口 | ✅ |
| WebView | pages/webview/index | 支付中间页 | - |

### 4.2 分包页面

#### 商品模块 (pkg-product)

| 页面 | 路径 | 功能 |
|------|------|------|
| 商品列表 | pkg-product/pages/list | 分类下的商品列表，支持下拉刷新 |
| 商品详情 | pkg-product/pages/detail | 商品信息、加入购物车、立即购买 |

#### 订单模块 (pkg-order)

| 页面 | 路径 | 功能 |
|------|------|------|
| 结算 | pkg-order/pages/checkout | 选择地址、配送方式、确认订单 |
| 支付 | pkg-order/pages/payment | 选择支付方式（微信/支付宝/余额） |
| 支付结果 | pkg-order/pages/pay-result | 支付成功/失败结果页 |
| 我的订单 | pkg-order/pages/orders | 订单列表，按状态筛选 |
| 订单详情 | pkg-order/pages/order-detail | 订单信息、物流、操作 |

#### 售后模块 (pkg-after-sale)

| 页面 | 路径 | 功能 |
|------|------|------|
| 申请售后 | pkg-after-sale/pages/apply | 选择售后类型、上传凭证 |
| 售后列表 | pkg-after-sale/pages/list | 售后记录列表 |
| 售后详情 | pkg-after-sale/pages/detail | 售后进度跟踪 |

#### 营销模块 (pkg-promotion)

| 页面 | 路径 | 功能 |
|------|------|------|
| 限时秒杀 | pkg-promotion/pages/flash-sale | 秒杀活动列表和倒计时 |
| 拼团 | pkg-promotion/pages/group-buy | 拼团活动和开团 |
| 优惠券 | pkg-promotion/pages/coupons | 优惠券领取和管理 |

#### 用户模块 (pkg-user)

| 页面 | 路径 | 功能 |
|------|------|------|
| 个人中心 | pkg-user/pages/profile | 用户资料编辑 |
| 地址管理 | pkg-user/pages/addresses | 收货地址增删改 |
| 充值卡 | pkg-user/pages/recharge | 充值卡兑换 |
| 余额明细 | pkg-user/pages/balance-history | 余额收支记录 |
| 分销中心 | pkg-user/pages/distribution | 分销收益、邀请好友 |
| 物流跟踪 | pkg-user/pages/logistics | 物流轨迹查询 |

### 4.3 TabBar 配置

底部导航 4 个 Tab 页：首页、分类、购物车、我的。

图标路径：`static/tab/` 目录。

---

## 5. 微信调试

### 5.1 微信开发者工具

1. 下载 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2. 导入项目：选择 `dist/dev/mp-weixin/` 目录
3. 在"详情" → "本地设置"中启用"不校验合法域名"（开发环境）

### 5.2 H5 微信浏览器调试

1. 启动 H5 开发服务器
2. 在微信开发者工具中选择"公众号网页"模式
3. 输入 H5 开发地址（如 http://localhost:5173）
4. **注意**：本地开发无法使用微信 OAuth，需要配置内网穿透或在测试环境验证

### 5.3 JS-SDK 调试

在 `utils/wechat.ts` 中临时设置 `debug: true`：

```typescript
wx.config({
    debug: true,  // 开启调试，页面会弹窗显示配置结果
    appId: sig.appId,
    // ...
});
```

也可使用微信开发者工具的"微信 JS-SDK 调试"功能。

---

## 6. 登录流程说明

### 6.1 三种登录方式

| 方式 | 触发条件 | 流程 |
|------|---------|------|
| 微信 H5 登录 | 微信浏览器 + AppID 已配置 | OAuth 授权 → 获取 code → authenticate |
| 手机验证码登录 | 点击"手机号登录" | 发送验证码 → 输入验证码 → authenticate |
| 账号密码登录 | 点击"账号密码登录" | 输入用户名密码 → login |

### 6.2 H5 微信浏览器自动登录

```
用户打开页面
    │
    ├─ 已登录（authStore.token 有值）→ 正常使用
    │
    └─ 未登录 + 微信浏览器 + AppID 已配置
        │
        └─→ 自动跳转微信 snsapi_base 授权页
            │
            └─→ 微信重定向回页面（带 code + state=wechat_base）
                │
                └─→ onMounted 检测 → handleWechatH5Callback → authenticate
                    │
                    └─→ 登录成功 → 跳转首页
```

### 6.3 手动微信登录

点击"微信登录"按钮触发 `snsapi_userinfo` 授权，弹窗询问用户是否授权获取昵称头像。

---

## 7. 分享功能

### 7.1 H5 分享

在微信浏览器中，页面可被分享到好友或朋友圈。

使用方式：

```typescript
// 在页面 setup 中调用
import { useShare } from '@/composables/useShare';

useShare({
    title: '分享标题',
    path: '/pages/home/index',
    imageUrl: 'https://...',
});

// 商品分享
import { useProductShare } from '@/composables/useShare';
useProductShare('商品名称', 'product-slug', 'https://image.url');
```

### 7.2 小程序分享

小程序使用原生分享 API，`useShare` 内部自动处理：
- `onShareAppMessage` — 分享给好友
- `onShareTimeline` — 分享到朋友圈

### 7.3 邀请码自动携带

分享链接自动拼接邀请码参数：

```
原始链接: https://shop.com/#/pkg-product/pages/detail?slug=xxx
分享链接: https://shop.com/#/pkg-product/pages/detail?slug=xxx&ref=USER_CODE
```

好友打开链接 → App.vue 捕获 `ref` → 存入 authStore.inviteCode。

---

## 8. 发布流程

### 8.1 H5 发布

```bash
# 构建
npm run build:h5

# 部署 dist/build/h5/ 目录到 Web 服务器
# 配置 Nginx 反向代理到后端 API
```

Nginx 配置示例：

```nginx
server {
    listen 443 ssl;
    server_name shop.example.com;
    root /var/www/vshop/dist/build/h5;
    index index.html;

    location /shop-api {
        proxy_pass http://localhost:3000;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### 8.2 小程序发布

```bash
# 构建
npm run build:mp-weixin

# 用微信开发者工具打开 dist/build/mp-weixin/
# 点击"上传" → 填写版本号 → 提交审核
```

### 8.3 微信平台配置

发布前需在微信公众平台配置：

| 配置项 | 位置 | 值 |
|--------|------|-----|
| 网页授权域名 | 公众号 → 开发 → 接口权限 → 网页授权 | shop.example.com |
| JS 安全域名 | 公众号 → 设置 → 功能设置 → JS接口安全域名 | shop.example.com |
| request 合法域名 | 小程序 → 开发 → 开发管理 → 服务器域名 | https://api.example.com |
| 业务域名 | 小程序 → 开发 → 开发管理 → 业务域名 | shop.example.com |
