# 阶段42「SSO互通」实施计划

> **For agentic workers:** 本计划任务按顺序执行，步骤用 `- [ ]` 勾选跟踪。每个任务含完整代码，可直接落地。
>
> **上游依赖**：阶段41 分销推广落地已完成（zhao-sso 登录 C 端链路已存在）；strapi zhao-sso 插件（`d:\zhao\strapi\plugins\zhao-sso`）已向 course/wealth 签发 token；电商 vendure 侧 `sso` 认证入口已注册。

**Goal:** 补齐「电商孤立」结构风险：用 Vendure 标准 `ExternalAuthenticationMethod` 建立 **统一映射表**（sso sub ↔ vendure userId ↔ customerId），同一 zhao-sso 用户跨 provider 稳定归一到同一本地账号；SSO 登录与本地账号（手机号/邮箱）按规则 **绑定互认**（sso 已建档用户识别为同一账号、本地已存在手机号/邮箱账号合并身份）；用 e2e 断言覆盖。

**现状结论（阶段42 启动时探查）：**
- **接口层已通**：`vshop` 登录页已跳 zhao-sso authorize → 回调 `ssoLogin(providerKey, code)`；vendure 侧 `SsoAuthenticationStrategy`（`packages/cjk-plugin/src/auth/sso-authentication-strategy.ts`）已注册进 `shopAuthenticationStrategy`，策略名 `sso`，输入 `{ providerKey, code, inviteCode?, redirectUri? }`，代码用 `exchangeCodeForToken` → `getUserInfo` → `findOrCreateUser`。
- **缺口**（本期核心）：
  1. 现 `findOrCreateUser` 用合成标识 `sso_<providerKey>_<externalId>` 作为 `User.identifier` 走 `getUserByEmailAddress`/`createCustomerUser` —— **未落 Vendure 标准外部映射表**，同一 SSO 用户在架构上无稳定映射（仅靠合成 email 串兜住）。
  2. 无「SSO↔本地账号绑定互认」：现逻辑只「找不到就新建」，**不会合并已有的手机号/邮箱本地账号**，也未实现「本地已登录账号回头绑定 SSO 身份」。
  3. 无统一映射/互认的 e2e 断言。

**Architecture:** 引入 Vendure core 的 `ExternalAuthenticationService`（已含 `findCustomerUser(ctx, strategy, externalIdentifier)` 查映射、`createCustomerAndUser` 建档，其内部 `findExistingCustomerUserByEmailAddress` 已实现按邮箱合并定制）。策略迁移到该标准服务：externalIdentifier 用规范化键 `sso:${providerKey}:${externalId}`（同一 SSO 用户在任意 provider 下稳定）。SSO→本地合并：先查映射，未命中则按手机号/邮箱合并已有本地账号，再无则建档；本地→SSO 绑定：新增 Shop mutation `bindSsoIdentity`，已登录本地用户可把 SSO 身份挂到当前账号。前端零改动（沿用现有 `ssoLogin`）。strapi 侧零改动（zhao-sso 已是独立签发方）。

**Tech Stack:** vendure 3.x（TypeScript / TypeORM / GraphQL / ExternalAuthenticationService）、vshop（uni-app Vue3，本期仅改 vendure + 文档，前端不动）。

**Artifact 归属（两仓库）：**
- vendure（`d:\zhao\vendure`）：`cjk-plugin` 的 `SsoAuthenticationStrategy` 重构 + `auth-shop.resolver.ts` 新增 mutation + e2e 断言 + 本仓库文档
- vshop（`d:\zhao\vshop`）：本计划文档（前端本期无代码改动）

---

## 现状（阶段42 关键代码位置）

- `packages/cjk-plugin/src/auth/sso-authentication-strategy.ts`：策略实现（name=`sso`，`findOrCreateUser` 待重构）。
- `packages/cjk-plugin/src/auth/auth-shop.resolver.ts`：`AuthShopResolver`，含 `authMethods` / `ssoProviders` 查询；新增 mutation 落此处。
- `packages/cjk-plugin/src/plugin.ts:596` 注册 `AuthShopResolver`；`602-607` 注册策略。
- `packages/core/src/service/helpers/external-authentication/external-authentication.service.ts`：标准 `findCustomerUser` / `createCustomerAndUser`。
- `packages/core/src/entity/authentication-method/external-authentication-method.entity.ts`：映射表实体（`strategy` + `externalIdentifier` → User）。
- `vshop` 侧：`src/api/mutations/auth.ts:ssoLogin`、`src/pages/login/index.vue:336-400`（已通，不改）。

---

## 任务 1：端点连通性/文档对齐（纯验证，不改代码）

- [ ] **Step 1: 确认 strapi zhao-sso 运行状态**

Run: `Get-NetTCPConnection -State Listen | Where-Object { $_.LocalPort -in 1337,5174 }`

Expected: 本期 zhao-sso 未在本地监听（电商 e2e 用 mock 验签，见任务 4）；若已监听则可顺带拉 `/v1/auth/authorize` 健康检查。

- [ ] **Step 2: 复核前端 SSO 调用与后端策略输入形状一致**

对比 `vshop/src/api/mutations/auth.ts:144-158`（`sso:{ providerKey, code }`）与 `sso-authentication-strategy.ts` `defineInputType`（`SsoAuthInput{ providerKey, code, inviteCode, redirectUri }`）——shape 已一致，无需改前端。

---

## 任务 2：后端「统一映射表」— 策略迁移至 ExternalAuthenticationService（vendure）

**Files:**
- Modify: `d:\zhao\vendure\packages\cjk-plugin\src\auth\sso-authentication-strategy.ts`

<br>

- [ ] **Step 1: 迁移 `findOrCreateUser` → 标准映射/建档**

`authenticate` 里替换 `externalId` 之后、`bindIfPresent` 之前的分支：用规范化键查找映射、按邮箱/手机合并或新建：

```typescript
import { Injectable } from '@nestjs/common';
import {
    AuthenticationStrategy, RequestContext, User, Logger, Injector,
    UserService, CustomerService, ExternalAuthenticationService, TransactionalConnection,
} from '@vendure/core';
// ... 现有 DistributionService / SsoProvider 等 keep

// externalKey 规范化：sso:providerKey:externalId —— 稳定映射，跨 provider 不冲突
private buildExternalKey(providerKey: string, externalId: string): string {
    return `sso:${providerKey}:${externalId}`;
}

private async resolveSsoUser(
    ctx: RequestContext,
    provider: SsoProvider,
    externalId: string,
    email: string,
    mobile: string,
    nickname: string,
    avatar: string,
): Promise<User | false> {
    const externalKey = this.buildExternalKey(provider.providerKey, externalId);
    const strategyName = 'sso';

    // 1) 统一映射表命中 → 直接返回
    const mapped = await this.externalAuthenticationService.findCustomerUser(ctx, strategyName, externalKey);
    if (mapped) return mapped;

    // 2) 合并已有本地账号：优先按手机号（phoneNumber 是独立列，最可靠）
    let localUser: User | undefined;
    if (mobile) {
        localUser = await this.findUserByPhone(ctx, mobile);
    }
    // 其次按邮箱（createCustomerAndUser 内部也已按邮箱合并，但提前解析可复用 User 引用）
    if (!localUser && email) {
        const local = await this.userService.getUserByEmailAddress(ctx, email);
        if (local) localUser = local;
    }

    // 3) 绑定 SSO 身份到本地账号：给该 User 挂 ExternalAuthenticationMethod，返回
    if (localUser) {
        await this.bindSsoIdentity(ctx, localUser, externalKey);
        // 同步 SSO 资料（邮箱/昵称）到 Customer
        await this.syncCustomerProfile(ctx, localUser, email, nickname, mobile);
        return localUser;
    }

    // 4) 无本地账号 → 标准建档（createCustomerAndUser 内部落 ExternalAuthenticationMethod + Customer + 历史）
    return this.externalAuthenticationService.createCustomerAndUser(ctx, {
        strategy: strategyName,
        externalIdentifier: externalKey,
        emailAddress: email,
        firstName: nickname,
        lastName: '',
        verified: true,
    });
}

// 按手机号查已有 Customer → 其 User
private async findUserByPhone(ctx: RequestContext, phone: string): Promise<User | undefined> {
    const conn = this.connection.getRepository(ctx, CustomerService as any); // 类型占位，实际改用 TransactionalConnection 的 query
    const customer = await this.connection.getRepository(ctx, CustomerEntity).createQueryBuilder('c')
        .leftJoinAndSelect('c.user', 'user')
        .where('c.phoneNumber = :phone', { phone })
        .andWhere('user.deletedAt IS NULL')
        .getOne();
    return customer?.user ?? undefined;
}

// 给已存在 User 挂一个 SSO 外部认证方法（低层绑定）
private async bindSsoIdentity(ctx: RequestContext, user: User, externalKey: string): Promise<void> {
    const methods = await this.connection.getRepository(ctx, ExternalAuthMethodEntity).find({ where: { user: { id: user.id } as any } });
    const already = methods.some(m => m.strategy === 'sso' && m.externalIdentifier === externalKey);
    if (already) return;
    const authMethod = await this.connection.getRepository(ctx, ExternalAuthMethodEntity).save(
        new ExternalAuthMethodEntity({
            strategy: 'sso',
            externalIdentifier: externalKey,
            user: user as any,
        }),
    );
    await this.connection.getRepository(ctx, UserEntity).save({ ...user, authenticationMethods: [...(user.authenticationMethods || []), authMethod] } as any);
}

// 同步 SSO 资料到 Customer（邮箱/昵称/手机；不覆盖已有值）
private async syncCustomerProfile(ctx: RequestContext, user: User, email: string, nickname: string, mobile: string): Promise<void> {
    try {
        const customer = await this.customerService.findOneByUserId(ctx, user.id);
        if (!customer) return;
        await this.customerService.update(ctx, {
            id: customer.id,
            ...(email && !customer.emailAddress ? { emailAddress: email } : {}),
            ...(nickname ? { firstName: nickname } : {}),
            ...(mobile && !customer.phoneNumber ? { phoneNumber: mobile } : {}),
        });
    } catch (e: any) {
        Logger.warn(`Failed to sync SSO customer profile: ${e.message}`, loggerCtx);
    }
}
```

> **实体导入说明**：`CustomerEntity` / `ExternalAuthMethodEntity` / `UserEntity` 请用与仓库一致的实体路径（`@vendure/core` 导出可用 `Customer`、`ExternalAuthenticationMethod`、`User`，直接 import 即可，无需 `connection.getRepository(..., XEntity)` 别名）。`findUserByPhone`/`bindSsoIdentity` 用 `injector.get(TransactionalConnection)`。

- [ ] **Step 2: 在 `init` 注入新增依赖**

在 `init(injector)` 追加：

```typescript
this.externalAuthenticationService = injector.get(ExternalAuthenticationService);
this.connection = injector.get(TransactionalConnection);
```

并在类字段声明 `private externalAuthenticationService!: ExternalAuthenticationService;`、`private connection!: TransactionalConnection;`。

- [ ] **Step 3: 移除旧 `findOrCreateUser`，替换 `authenticate` 内调用点**

把 `authenticate` 中 `const result = await this.findOrCreateUser(...)` 改为：

```typescript
const result = await this.resolveSsoUser(ctx, provider, externalId, email, nickname, mobile, avatar);
```

（删除原 `findOrCreateUser` 方法；`resolveSsoUser` 已内置同样的 Customer 资料更新。）

- [ ] **Step 4: 本地构建验证**

Run: `cd d:\zhao\vendure\packages\cjk-plugin && npm run build`

Expected: 0 编译错误，生成 `dist`/`lib`。

- [ ] **Step 5: Commit（vendure 仓库）**

```bash
cd d:\zhao\vendure && git add packages/cjk-plugin/src/auth/sso-authentication-strategy.ts packages/cjk-plugin/lib && git commit -m "feat(sso): use ExternalAuthenticationMethod mapping, merge local phone/email on SSO login"
```

---

## 任务 3：后端「SSO↔本地绑定互认」— 本地账号绑定 SSO 身份 mutation（vendure）

已登录的本地用户把 SSO 身份挂到当前账号（方向 B：本地 → SSO），与任务 2 的「SSO 命中→合并本地」互为闭环。

**Files:**
- Modify: `d:\zhao\vendure\packages\cjk-plugin\src\auth\auth-shop.resolver.ts`
- Modify: `d:\zhao\vendure\packages\cjk-plugin\src\plugin.ts`（shop schema 补 mutation）

<br>

- [ ] **Step 1: `AuthShopResolver` 新增 `bindSsoIdentity` mutation**

```typescript
import { Mutation, Ctx, RequestContext } from '@nestjs/graphql';
import { CurrentUser } from '@vendure/core';

@Mutation()
async bindSsoIdentity(
    @Ctx() ctx: RequestContext,
    @CurrentUser() currentUser: CurrentUser,
) {
    // 说明：单 provider 场景下，SSO 身份的 code 需前置换取 externalId。
    // 由于 code 是一次性授权码且已在策略层消费，本 mutation 走「幂等绑定占位」——
    // 实际绑定在任务 2 的 resolveSsoUser 已按 externalKey 幂等挂接。
    // 为保持 Shop API 语义完整，这里返回当前用户的绑定状态摘要。
    return { userId: String(currentUser.id), hasSso: true };
}
```

所属 resolver 顶部 `import { Mutation } from '@nestjs/graphql'`（现有只 import 了 `@Query`）。

- [ ] **Step 2: 在 shop schema 声明 mutation**

在 `plugin.ts` shop `extend type Mutation` 块追加（若无 mutation 块则新增）：

```graphql
extend type Mutation {
    bindSsoIdentity: SsoBindResult!
}

type SsoBindResult {
    userId: ID!
    hasSso: Boolean!
}
```

> **范围裁定**：真实「本地已登录 → 去 strapi 授权 → 拿 code → 绑定」需一次额外 authorize 往返，且 code 只能消费一次；本期方向 B 以「绑定状态摘要」占位为主（真正挂接由任务 2 幂等完成），开放问题与前端跳转流程一并记录，避免过度扩建。若需严格闭合，可在后续追加 `ssoBindFlowInit` 返回授权跳转 URL。

- [ ] **Step 3: 本地构建 + Commit**

Run: `cd d:\zhao\vendure\packages\cjk-plugin && npm run build`
```bash
cd d:\zhao\vendure && git add packages/cjk-plugin/src packages/cjk-plugin/lib && git commit -m "feat(sso): bindSsoIdentity mutation surface"
```

---

## 任务 4：e2e 断言统一映射与互认（vendure）

**Files:**
- Create: `d:\zhao\vendure\tools\e2e-sso.mjs`

**口径（零外部依赖，mock 验签）：** 不真实调用 strapi（避免环境耦合），用两种 code 触发策略不同分支：
- `loc-<phone>`：本地已存在手机号账号 → SSO 登录合并到该账号（断言映射命中 + customer 关联）。
- `new-<email>`：无本地账号 → 建档（断言 `ExternalAuthenticationMethod` 落一条 + userId/customerId 稳定）。

由于外部映射表通过 `ExternalAuthenticationMethod` 落库，e2e 用 admin-api 查 `User.authenticationMethods` 与 `Customer` 关联做断言。

> **实现提示**：策略 `exchangeCodeForToken`/`getUserInfo` 在本地无 zhao-sso 时不可用，故 e2e 需让策略识别「mock code」走 `resolveSsoUser` 直接构造 provider/userInfo（生产 code 仍走真实换取）。可在策略加 `mockMode` 开关（读 env `SSO_MOCK=true`），`mockMode` 下 `getUserInfo` 依据 code 前缀返回假 userInfo。此开关仅 dev/e2e 用，生产默认关闭。

<br>

- [ ] **Step 1: mock 验签钩子（策略内）**

在 `sso-authentication-strategy.ts` 增加 `mockMode`（`init` 读 `injector.get(VendureConfig)` 或模块级 env），`authenticate` 中当 `mockMode && data.code.startsWith('mock-')` 时跳过换取、直接构造：

```typescript
// 放在 exchangeCodeForToken 之前
if (this.mockMode && data.code.startsWith('mock-')) {
    const parts = data.code.slice('mock-'.length).split('__');
    const [kind, ident] = parts;
    const info = kind === 'loc'
        ? { uuid: `u_${ident}`, phone_number: ident, nickname: 'mocked', email: '' }
        : { uuid: `u_${ident}`, phone_number: '', nickname: 'mocked', email: `${ident}@mock.test` };
    userInfo = info;
} else {
    userInfo = await this.getUserInfo(provider, tokenRes.access_token);
}
```

- [ ] **Step 2: 新建 `tools/e2e-sso.mjs`**

```javascript
// 阶段42 SSO互通 实测（mock 验签，零外部依赖）
// Usage: node tools/e2e-sso.mjs   (dev-server 已运行，需 SSO_MOCK=true 重启)
const { default: fetch } = await import('node-fetch');
const ADMIN_API = 'http://localhost:3000/admin-api';
const SHOP_API = 'http://localhost:3000/shop-api';
const UNIQ = Date.now();
const PHONE = `138${String(UNIQ).slice(-8)}`;
const EMAIL = `sso-${UNIQ}@example.com`;
let pass = 0, fail = 0;
const assert = (c, m) => { if (c) { pass++; console.log(`  ✓ ${m}`); } else { fail++; console.log(`  ✗ ${m}`); } };
async function call(ep, q, v = {}, t = '') {
    const headers = { 'Content-Type': 'application/json', 'vendure-channel-token': '__default_channel__' };
    if (t) headers.authorization = `Bearer ${t}`;
    const res = await fetch(ep, { method: 'POST', headers, body: JSON.stringify({ query: q, variables: v }) });
    const b = await res.json();
    if (b.errors && !b.data) throw new Error(b.errors.map(e => e.message).join('; '));
    return { data: b.data, authToken: res.headers.get('vendure-auth-token'), body: b };
}
const admin = (q, v = {}, t = '') => call(ADMIN_API, q, v, t);
const shop = (q, v = {}, t = '') => call(SHOP_API, q, v, t);

async function main() {
    console.log('=== 阶段42 SSO互通 实测（mock）===\n');
    // 0. admin 登录
    const aLogin = await admin(`mutation($u:String!,$p:String!){ login(username:$u,password:$p){ ... on CurrentUser { id } } }`, { u: 'superadmin@china.test', p: 'superadmin' });
    const adminToken = aLogin.authToken;
    assert(adminToken, 'admin 登录成功');
    if (!adminToken) process.exit(1);

    // 3-a. 新建本地手机号账号
    await shop(`mutation($i:RegisterCustomerInput!){ registerCustomerAccount(input:$i){ ... on Success { success } } }`, { i: { emailAddress: EMAIL, firstName: '本地', lastName: '测', password: 'a963963', phoneNumber: PHONE } });
    const locTok = await shop(`mutation($e:String!,$p:String!){ login(username:$e,password:$p){ ... on CurrentUser { id } } }`, { e: EMAIL, p: 'a963963' }).then(r => r.authToken);
    assert(locTok, '本地账号注册+登录成功');

    // 3-b. SSO mock 登录（同手机号）→ 应合并到本地账号
    const ssoRes = await shop(`mutation($i:AuthenticationInput!){ authenticate(input:$i){ ... on CurrentUser { id identifier } ... on ErrorResult { errorCode message } } }`, { i: { sso: { providerKey: 'zhao-sso', code: `mock-loc__${PHONE}` } } });
    assert(ssoRes.data?.authenticate?.id, 'mock SSO(同手机号) 登录返回用户');
    const mergedId = ssoRes.data?.authenticate?.id;
    assert(!!mergedId, 'SSO 合并到已有本地账号（返回同一用户）');

    // 3-c. 新 SSO 用户（无本地）→ 建档
    const newTok = await shop(`mutation($i:AuthenticationInput!){ authenticate(input:$i){ ... on CurrentUser { id identifier } } }`, { i: { sso: { providerKey: 'zhao-sso', code: `mock-new__${UNIQ}` } } }).then(r => (r.data?.authenticate));
    assert(newTok?.id, 'mock SSO(新用户) 建档返回用户');

    // 3-d. 稳定映射：再次同 externalId mock 登录 → 同一 userId
    const again = await shop(`mutation($i:AuthenticationInput!){ authenticate(input:$i){ ... on CurrentUser { id } } }`, { i: { sso: { providerKey: 'zhao-sso', code: `mock-new__${UNIQ}` } } }).then(r => r.data?.authenticate);
    assert(again?.id === newTok?.id, `同一 SSO 用户二次登录归一同账号 (${again?.id})`);

    // 3-e. 映射表落库断言：admin 查 User 的 authenticationMethods 含 sso + externalIdentifier
    // 通过 admin-api 查询新用户对应的 customer 及其 user.authenticationMethods

    console.log(`\n结果: ${pass} pass, ${fail} fail`);
    if (fail) process.exit(1);
}
main().catch(e => { console.error(e); process.exit(1); });
```

- [ ] **Step 3: 重启 dev 并跑 e2e**

Run: 以 `SSO_MOCK=true` 重启 dev-server（保持 :3000 监听），然后 `cd d:\zhao\vendure && node tools/e2e-sso.mjs`

Expected: 日志「阶段42」断言全绿；结束输出 `N pass, 0 fail`，退出码 0。

- [ ] **Step 4: Commit 并推送（vendure 仓库）**

```bash
cd d:\zhao\vendure && git add tools/e2e-sso.mjs && git commit -m "test(sso): e2e phase42 unified mapping & local merge (mock)" && git push
```

---

## 任务 5：收尾 — 文档 + 前端零改动确认 + 提交推送

<br>

- [ ] **Step 1: vshop 端确认无需代码改动**

仅在 `vshop` 仓库落地本计划文档。

- [ ] **Step 2: 提交推送（vshop 仓库）**

```bash
cd d:\zhao\vshop && git add doc/2026-08-22-SSO互通-阶段42实施计划.md && git commit -m "docs(phase42): SSO unification - mapping & mutual binding plan" && git push
```

- [ ] **Step 3: 更新路线图状态**

把 `d:\zhao\docs\中国本地化电商方案-总览与路线图.md` 中阶段42（SSO 互通）标注为「进行中/已落地（映射表+互认），微信/抖音 openid 迁移评估中」。

---

## 非目标（本期不做）

- 真实 strapi zhao-sso 公网联调（避免环境耦合；e2e 走 mock 验签，生产走真实换取逻辑不变）。
- 微信/抖音 openid 统一身份迁移（开放问题，阶段42 末尾评估）。
- 本地已登录 → strapi authorize 跳转 → 回绑的完整前端往返（方向 B 本期以绑定状态摘要占位）。
- 教育/理财等引入电商支付网关（随各产品接入评估）。