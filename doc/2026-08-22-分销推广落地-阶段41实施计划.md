# 阶段41「分销推广落地」实施计划

> **For agentic workers:** 本计划任务按顺序执行，步骤用 `- [ ]` 勾选跟踪。每个任务含完整代码，可直接落地。
>
> **上游依赖**：阶段40 分销结算可视化（distribution-plugin 佣金/提现已闭环，`referralCode`/`referredBy` 已打通到 C 端分销中心）。阶段37 券体系（`totalWithTax` 佣金基数）。

**Goal:** 分销员可生成专属推广链接与推广海报分享给好友；好友通过独立邀请落地页打开并绑定推荐关系，下单后按既有佣金链路归因；分销中心新增「我的团队」与推广效果视图，整体形成「分享 → 绑定 → 下单 → 佣金 → 团队」的分销推广闭环。

**Architecture:** 后端在 `distribution-plugin` 新增 `myTeamSummary` 聚合查询（从 Distributor 的 parentId 链 + 当前分销员的 CommissionRecord 聚合，零埋点、无新表）；前端全在 `vshop`：新增统一 `ref` 解析器、独立邀请落地页 `pages/landing/invite`，分销中心扩展「推广/团队/海报」区，海报基于现有 `product-poster` 二维码能力自建分销专属模板。**不接 strapi 广告位/发布中心**（与商品/分销无数据衔接）。

**Tech Stack:** vendure 3.x（TypeScript / TypeORM / GraphQL）、vshop（uni-app Vue3）、`qrcode`(H5 二维码)、`getWxacode`(小程序码)。

**Artifact 归属（两仓库）：**
- vendure（`d:\zhao\vendure`）：`myTeamSummary` 的 schema + service + resolver + e2e 断言
- vshop（`d:\zhao\vshop`）：`ref` 解析工具、邀请落地页、分销中心推广/团队区、分销推广海报、API 封装、本计划文档

---

## 现状（阶段41 启动时探查）

**后端 `distribution-plugin`（`d:\zhao\vendure\packages\distribution-plugin`）：**
- `Distributor`：`parentId`（上级分销员）、`level`（1/2/3，最大3层）、`referralCode`、状态。
- `CommissionRecord`：`distributorId`(获佣者)、`orderId`、`commissionType`(direct/indirect)、`orderAmount`、`commissionAmount`、`status`(pending/confirmed/paid/cancelled)。直推=给一级邀请人，间推=给二级。**团队成员的下单会为当前分销员产生 direct/indirect 记录。**
- `DistributionShopResolver`：`myDistributorProfile/myCommissionRecords/myWithdrawalRequests/applyDistributor/requestWithdrawal`，已有 `resolveCustomerId`（User id → Customer id）。
- Shop schema 定义在 `plugin.ts` 的 `shopApiExtensions.schema`。

**前端 `vshop`：**
- `src/stores/auth.ts`：`setInviteCode(code)` 写 `authStore.inviteCode` + storage `auth_inviteCode`。
- `src/pages/login/index.vue:402`：`tryUpdateReferredBy(inviteCode)` 登录后把码经 `updateCustomer(customFields.referredBy)` 落库（仅原值非空时跳过）。
- `src/components/product-poster/usePosterData.ts`：H5 用 `qrcode` 生成带 `&ref=` 的 URL 二维码；小程序用 `getWxacode(scene)` 生成小程序码。
- `src/pkg-user/pages/distribution.vue`：未申请引导 + 已申请（概况/推荐码复制/佣金/提现）。

**缺口：**
1. 无「解析分享 URL `ref` → setInviteCode →（已登录补写 referredBy）」的统一入口；现只有登录页登录后补写。
2. 无独立的邀请落地页（好友打开链接落入商城首页/商品页，绑定不够专业可控）。
3. 分销中心无「推广链接/海报/我的团队/推广效果」模块。
4. 后端无 `myTeamSummary` 聚合查询。

---

## 任务 1：后端 `myTeamSummary` 聚合查询（vendure）

**Files:**
- Modify: `d:\zhao\vendure\packages\distribution-plugin\src\plugin.ts`（shop schema 新增 `TeamSummary` 类型 + Query）
- Modify: `d:\zhao\vendure\packages\distribution-plugin\src\distribution.service.ts`（新增 `getTeamSummary`）
- Modify: `d:\zhao\vendure\packages\distribution-plugin\src\distribution-shop.resolver.ts`（新增 `myTeamSummary` resolver）
- Test: `d:\zhao\vendure\tools\e2e-distribution.mjs`（追加团队聚合断言）

**口径（零埋点）：**
- `directTeamSize` = 分销员数 `parentId = 当前id`
- `indirectTeamSize` = 分销员数 `parentId ∈ 直推 ids`
- `orderCount` = 当前分销员名下 `confirmed/paid` 佣金记录的去重订单数
- `orderAmount` = 这些记录的 `orderAmount` 求和
- `teamCommission` = 这些记录的 `commissionAmount` 求和
- 均在查询中显式按当前 channel 过滤（raw `createQueryBuilder` 不会自动带 channel 条件）。

<br>

- [ ] **Step 1: 在 `distribution.service.ts` 新增 `getTeamSummary`**

在 `DistributionService` 内新增方法（放在 `generateReferralCode` 之前）、并导出类型。因 `Distributor` 是 `ChannelAware` 且 `getRepository(ctx)` 返回的 raw `createQueryBuilder` **不会自动拼 channel 条件**，需与 `findByCustomerId`（`innerJoin('d.channels',...)` + `channel.id = :channelId`）一致，显式按当前 channel 过滤：

```typescript
export interface TeamSummary {
    directTeamSize: number;
    indirectTeamSize: number;
    totalTeamSize: number;
    orderCount: number;
    orderAmount: number;
    teamCommission: number;
}

async getTeamSummary(ctx: RequestContext, distributorId: ID): Promise<TeamSummary> {
    const repo = this.connection.getRepository(ctx, Distributor);
    const channelId = ctx.channelId;

    // 直推下级：parentId = 当前分销员（限当前 channel）
    const directChildren = await repo
        .createQueryBuilder('d')
        .innerJoin('d.channels', 'channel')
        .where('d.parentId = :pid', { pid: String(distributorId) })
        .andWhere('channel.id = :channelId', { channelId })
        .getMany();
    const directIds = directChildren.map(c => c.id);
    const directTeamSize = directChildren.length;

    // 间推下级：parentId ∈ 直推 ids（限当前 channel）
    let indirectTeamSize = 0;
    if (directIds.length) {
        indirectTeamSize = await repo
            .createQueryBuilder('d')
            .innerJoin('d.channels', 'channel')
            .where('d.parentId IN (:...ids)', { ids: directIds.map(String) })
            .andWhere('channel.id = :channelId', { channelId })
            .getCount();
    }

    // 当前分销员带来的订单与收益（仅 confirmed/paid，避免 pending 未结算虚高）
    const crRepo = this.connection.getRepository(ctx, CommissionRecord);
    const rows = await crRepo
        .createQueryBuilder('cr')
        .select('COUNT(DISTINCT cr.orderId)', 'orderCount')
        .addSelect('COALESCE(SUM(cr.orderAmount),0)', 'orderAmount')
        .addSelect('COALESCE(SUM(cr.commissionAmount),0)', 'commission')
        .where('cr.distributorId = :did', { did: String(distributorId) })
        .andWhere('cr.status IN (:...s)', { s: ['confirmed', 'paid'] })
        .getRawOne();

    return {
        directTeamSize,
        indirectTeamSize,
        totalTeamSize: directTeamSize + indirectTeamSize,
        orderCount: Number(rows?.orderCount ?? 0),
        orderAmount: Number(rows?.orderAmount ?? 0),
        teamCommission: Number(rows?.commission ?? 0),
    };
}
```

在文件头部 import 增加 `CommissionRecord`：

```typescript
import { CommissionRecord } from './commission-record.entity';
```

（`Channel` 已在顶部 `@vendure/core` import 中，无需新增。）

- [ ] **Step 2: 在 `plugin.ts` shop schema 新增 `TeamSummary` 类型与 Query**

在 `shopApiExtensions.schema` 内（`CommissionRecord` 类型之后）添加：

```graphql
type TeamSummary {
    directTeamSize: Int!
    indirectTeamSize: Int!
    totalTeamSize: Int!
    orderCount: Int!
    orderAmount: Int!
    teamCommission: Int!
}
```

在 `extend type Query` 块内（`myWithdrawalRequests` 之后）添加：`myTeamSummary: TeamSummary!`

- [ ] **Step 3: 在 `distribution-shop.resolver.ts` 新增 resolver**

在 `DistributionShopResolver` 类内新增方法（放在 `myWithdrawalRequests` 之后）：

```typescript
@Query()
async myTeamSummary(@Ctx() ctx: RequestContext): Promise<{ directTeamSize: number; indirectTeamSize: number; totalTeamSize: number; orderCount: number; orderAmount: number; teamCommission: number }> {
    const customerId = await this.resolveCustomerId(ctx);
    if (!customerId) return { directTeamSize: 0, indirectTeamSize: 0, totalTeamSize: 0, orderCount: 0, orderAmount: 0, teamCommission: 0 };
    const distributor = await this.distributionService.findByCustomerId(ctx, customerId);
    if (!distributor) return { directTeamSize: 0, indirectTeamSize: 0, totalTeamSize: 0, orderCount: 0, orderAmount: 0, teamCommission: 0 };
    return this.distributionService.getTeamSummary(ctx, distributor.id);
}
```

- [ ] **Step 4: 重启 dev、构建插件验证 schema 与查询**

Run: `cd d:\zhao\vendure\packages\distribution-plugin && npm run build`
Run: 重启 dev-server 后，用已通过阶段40的日志中的 shop-api（经 `tools/e2e-distribution.mjs`）——直接进入 Step 5 覆盖验证。

- [ ] **Step 5: 在 `tools/e2e-distribution.mjs` 追加团队聚合断言**

在「9. 拒绝链」之前插入如下断言（复用该文件内已登录的 `xTok`：X 是一级邀请 A，A 是 X 的直推；B 经 A 从 X 间推）：

```javascript
// 9. 团队汇总：X 有直推 A；B 经 A 下单为 X 生成 indirect 佣金（confirmed/paid 计入）
const team = await shop(`query { myTeamSummary { directTeamSize indirectTeamSize totalTeamSize orderCount orderAmount teamCommission } }`, {}, xTok);
const teamData = team.data?.myTeamSummary;
assert(teamData && teamData.directTeamSize >= 1, `X 团队直推=> ${teamData?.directTeamSize}`);
assert(teamData && teamData.totalTeamSize >= 1, `X 团队总 => ${teamData?.totalTeamSize}`);
// orderAmount 应包含 B 订单的间接佣金基数；请按实际期望核对，这里至少要求出现订单
assert(teamData && teamData.orderCount >= 1, `X 团队 orderCount => ${teamData?.orderCount}`);
assert(teamData && teamData.teamCommission >= expIndirect, `X 团队佣金 >= 间推佣金(${expIndirect}) => ${teamData?.teamCommission}`);
```

> 注：`expIndirect` 变量在该脚本前序已定义为间推佣金期望值。若 X 还有更早订单累计，用 `>=` 断言以放宽。

- [ ] **Step 6: 运行 e2e**

Run: `cd d:\zhao\vendure && node tools/e2e-distribution.mjs`
Expected: 原有 28 断言全绿 + 新增团队断言通过，退出码 0。

- [ ] **Step 7: Commit（vendure 仓库）**

```bash
cd d:\zhao\vendure && git add packages/distribution-plugin/src packages/distribution-plugin/lib tools/e2e-distribution.mjs
git commit -m "feat(distribution): myTeamSummary aggregate for promo team & effect"
```

---

## 任务 2：前端统一 `ref` 解析器（vshop）

统一把「分享链接/海报里的 `ref` 码」落到 `authStore.inviteCode`，并在当前已登录时补写 `customer.referredBy`。供邀请落地页（任务3）与分销中心分享调用。

**Files:**
- Create: `d:\zhao\vshop\src\utils\ref.ts`

<br>

- [ ] **Step 1: 新建 `utils/ref.ts`**

```typescript
import { useAuthStore } from '../stores/auth';
import { getGraphQLClient } from '../api/client';

/**
 * 解析并落址邀请码：
 * 1. 若 URL 带 ref 参数（H5）/ options.ref（小程序 onLoad），写入 authStore（含 storage）。
 * 2. 若当前已登录且客户尚未绑定 referredBy，就地补写。
 * 返回解析到的 ref 码（无则空串）。
 */
export async function parseAndBindRef(ref?: string): Promise<string> {
    const authStore = useAuthStore();
    const code = (ref && String(ref).trim()) || '';
    if (code) {
        authStore.setInviteCode(code);
    }
    const stored = authStore.inviteCode;
    if (!stored) return '';
    if (!authStore.isLoggedIn) return stored;

    try {
        const client = getGraphQLClient();
        const res: any = await client.request(`query {
            activeCustomer { id customFields { referredBy } }
        }`);
        if (res?.activeCustomer?.customFields?.referredBy) return stored;
        await client.request(`mutation UpdateCustomerReferredBy($referredBy: String!) {
            updateCustomer(input: { customFields: { referredBy: $referredBy } }) {
                ...on Customer { id }
                ...on ErrorResult { errorCode }
            }
        }`, { referredBy: stored });
    } catch (e) {
        console.error('parseAndBindRef 补写 referredBy 失败', e);
    }
    return stored;
}
```

> `getGraphQLClient` 已存在且导出于 `src/api/client.ts`（`distribution.ts` 顶部即 `import { getGraphQLClient } from '../client'`）。本文件位于 `src/utils/ref.ts`，与 `../api/client` 相对路径一致，可直接使用。

- [ ] **Step 2: Commit（vshop 仓库）**

```bash
cd d:\zhao\vshop && git add src/utils/ref.ts
git commit -m "feat(distribution): unified ref parser & bind helper"
```

---

## 任务 3：前端「邀请落地页」`pages/landing/invite`（vshop）

好友打开分销员分享的推广链接（H5 `/#/pages/landing/invite?ref=CODE`），展示邀请人信息 + 商城入口；`onLoad` 调用 `parseAndBindRef` 完成绑定。

**Files:**
- Create: `d:\zhao\vshop\src\pages\landing\invite.vue`
- Modify: `d:\zhao\vshop\src\pages.json`（注册路由）

<br>

- [ ] **Step 1: 新建 `pages/landing/invite.vue`**

```vue
<template>
  <view class="invite-page">
    <view class="invite-hero">
      <image v-if="avatar" class="invite-avatar" :src="avatar" mode="aspectFill" />
      <view class="invite-avatar invite-avatar--fallback" v-else>{{ inviterName.charAt(0) }}</view>
      <text class="invite-title">{{ inviterName }} 邀你一起逛商城</text>
      <text class="invite-code" v-if="inviteCode">推荐码：{{ inviteCode }}</text>
    </view>

    <view class="invite-actions">
      <button class="invite-btn" @click="goShopping">进商城逛逛</button>
      <text v-if="!isLoggedIn" class="invite-login" @click="goLogin">登录后绑定推荐关系</text>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue';
import { onLoad } from '@dcloudio/uni-app';
import { useAuthStore } from '../../stores/auth';
import { useTenantStore } from '../../stores/tenant';
import { parseAndBindRef } from '../../utils/ref';

const authStore = useAuthStore();
const tenantStore = useTenantStore();
const inviteCode = ref('');
const isLoggedIn = computed(() => authStore.isLoggedIn);

const inviterName = computed(() => tenantStore.tenantName || '好友');
const avatar = computed(() => ''); // 如需展示邀请人头像，可后续经 ref 反查；本期先用首字

function goShopping() {
    // 首页是 tabBar 页（pages.json tabBar.list[0].pagePath = pages/home/index），须用 switchTab
    uni.switchTab({ url: '/pages/home/index' });
}

function goLogin() {
    // 复用 authStore.requireLogin：未登录时跳转登录页并记住当前落地页，登录后被 tryUpdateReferredBy 落库
    authStore.requireLogin('/pages/landing/invite?ref=' + encodeURIComponent(inviteCode.value || ''));
}

onLoad(async (options) => {
    const ref = options?.ref ?? '';
    inviteCode.value = await parseAndBindRef(ref as string);
});
</script>

<style scoped>
.invite-page{display:flex;flex-direction:column;align-items:center;justify-content:center;min-height:100vh;padding:60rpx 40rpx;background:linear-gradient(180deg,#fff 0%,#f6f7fb 100%);}
.invite-hero{display:flex;flex-direction:column;align-items:center;margin-bottom:80rpx;}
.invite-avatar{width:160rpx;height:160rpx;border-radius:50%;background:#e8ecf2;margin-bottom:24rpx;}
.invite-avatar--fallback{display:flex;align-items:center;justify-content:center;font-size:56rpx;color:#fff;background:#ff7d00;}
.invite-title{font-size:36rpx;font-weight:600;color:#1a1a1a;}
.invite-code{margin-top:16rpx;font-size:26rpx;color:#999;background:#f1f1f5;padding:6rpx 24rpx;border-radius:999rpx;}
.invite-actions{width:100%;display:flex;flex-direction:column;align-items:center;}
.invite-btn{width:100%;height:88rpx;line-height:88rpx;background:linear-gradient(90deg,#ff7d00,#ff5c00);color:#fff;border-radius:999rpx;font-size:32rpx;}
.invite-login{margin-top:32rpx;font-size:26rpx;color:#666;text-decoration:underline;}
</style>
```

> 页面顶部 `#invite-hero` 已含 `invite-login` 的 `@click="goLogin"` 与"进商城逛逛"按钮；`goLogin` 复用 `authStore.requireLogin`（已在 `stores/auth.ts` 定义），登录后新客会经登录/注册流程的 `tryUpdateReferredBy` 落库；已登录客在落地页 `onLoad` 已由 `parseAndBindRef` 补写。首页为 tabBar 页，故进商城用 `uni.switchTab`。

- [ ] **Step 2: 在 `pages.json` 注册落地页**

在 `pages` 数组追加（按现有 tab/普通页组织）：

```json
{ "path": "pages/landing/invite", "style": { "navigationBarTitleText": "邀请" } }
```

- [ ] **Step 3: Commit（vshop 仓库）**

```bash
cd d:\zhao\vshop && git add src/pages/landing/invite.vue src/pages.json
git commit -m "feat(distribution): invite landing page with ref binding"
```

---

## 任务 4：分销推广海报（vshop，自建）

上传分销中心生成专属推广海报：昵称/推荐码 + 二维码（二维码含 `ref`，H5 跳落地页URL、小程序为小程序码带 scene），复用 `product-poster` 的二维码获取逻辑。

**Files:**
- Modify: `d:\zhao\vshop\src\components\product-poster\usePosterData.ts`（抽取/复用二维码逻辑，或新增分发入口）
- Create: `d:\zhao\vshop\src\components\dist-poster\dist-poster.vue`（海报画布）
- Modify: `d:\zhao\vshop\src\api\queries\distribution.ts`（若有复用 where 需导出）

<br>

- [ ] **Step 1: 新建可复用二维码工具 `src/utils/poster-qr.ts`**

从 `usePosterData.ts:35-42` 与 `19-32` 抽出「H5 URL 二维码」「小程序码」为独立工具（H5 落地页为 `/#/pages/landing/invite?ref=CODE`）：

```typescript
import { getWxacode } from '../api/queries/wechat';

// H5：生成指向落地页的 URL 二维码 base64（扫码后进入落地页并携带 ref 由 onLoad 绑定）
export async function generateH5InviteQr(inviteCode: string): Promise<string> {
    const { default: QRCode } = await import('qrcode');
    const shareUrl = `${window.location.origin}/#/pages/landing/invite?ref=${encodeURIComponent(inviteCode)}`;
    const dataUrl = await QRCode.toDataURL(shareUrl, { width: 200, margin: 1 });
    return dataUrl.split(',')[1];
}

// 小程序：生成小程序码（scene 为 ref，扫码后落地页 onLoad options 里无 scene 则需后端 scene 解析；本期小程序由 H5 跳转为主）
export async function generateMpInviteQr(inviteCode: string): Promise<string> {
    const res = await getWxacode(`r=${inviteCode}`);
    return res.base64;
}
```

> 微信端落地页 onLoad 解析 `scene` 超出本期范围时，可将小程序分享主推「分享卡片」直达商品/首页，海报主用于 H5 与二维码扫码。本期聚焦 H5 生成可用，小程序中的海报可复用现有的 `product-poster`。

- [ ] **Step 2: 新建 `components/dist-poster/dist-poster.vue`（H5 Canvas 渲染）**

一个轻量分享海报组件：接收 `channelName`、`inviterName`（分销员昵称，可先用推荐码或租户名）、`inviteCode`，canvas 绘制「渐变底 + 名字/码 + 二维码」。

```vue
<template>
  <view>
    <canvas canvas-id="distPoster" id="distPoster" class="dist-poster" />
    <view class="dist-poster-bar">
      <button class="dist-poster-save" @click="save">保存海报</button>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue';
import { generateH5InviteQr } from '../../utils/poster-qr';

const props = defineProps<{
  channelName: string;
  inviterName: string;
  inviteCode: string;
}>();

const posterDataUrl = ref('');

onMounted(async () => {
  const qr = await generateH5InviteQr(props.inviteCode);
  const q = uni.createCanvasContext('distPoster');
  const W = 600, H = 1000;
  // 背景
  const grad = q.createLinearGradient(0, 0, 0, H);
  grad.addColorStop(0, '#ff7d00');
  grad.addColorStop(1, '#ff3d00');
  q.setFillStyle(grad);
  q.fillRect(0, 0, W, H);
  // 文本
  q.setFillStyle('#ffffff');
  q.setFontSize(52); q.setTextAlign('center'); q.fillText(`${props.channelName}`, W / 2, 220);
  q.setFontSize(36); q.fillText(`${props.inviterName} 邀你一起逛`, W / 2, 300);
  q.setFillStyle('#ffe9d6'); q.setFontSize(28); q.fillText(`推荐码：${props.inviteCode}`, W / 2, 360);
  // 二维码
  q.drawImage(`data:image/png;base64,${qr}`, W / 2 - 140, 480, 280, 280);
  // 提示
  q.setFillStyle('#fff'); q.setFontSize(24); q.fillText('长按扫码进商城', W / 2, 860);
  q.draw().then && q.draw();
});
</script>

<style scoped>
.dist-poster{width:600rpx;height:1000rpx;border-radius:24rpx;overflow:hidden;}
.dist-poster-bar{padding:32rpx 0;}
.dist-poster-save{width:100%;height:88rpx;line-height:88rpx;background:#ff7d00;color:#fff;border-radius:999rpx;}
</style>
```

> 说明：canvas API 差异（H5 `uni.createCanvasContext` 与小程序一致，均可用于 silk 保存）。`save` 用 `uni.canvasToTempFilePath({ canvasId: 'distPoster' }, ()=>uni.saveImageToPhotosAlbum(...))`。以上为示意骨架，落地时按当前 `product-poster-h5.vue` 的具体绘制/保存写法对齐，避免重复造轮子。

- [ ] **Step 3: Commit（vshop 仓库）**

```bash
cd d:\zhao\vshop && git add src/components/product-poster/ src/components/dist-poster/
git commit -m "feat(distribution): self-built distribution promo poster"
```

---

## 任务 5：C 端分销中心扩展「推广 / 团队」区（vshop）

在 `distribution.vue` 的「已申请」态新增：专属推广链接生成/复制、海报入口（打开 dist-poster 或复用 product-poster 弹窗）、「我的团队」汇总（调 `myTeamSummary`）。

**Files:**
- Modify: `d:\zhao\vshop\src\api\queries\distribution.ts`（新增 `getMyTeamSummary`）
- Modify: `d:\zhao\vshop\src\pkg-user\pages\distribution.vue`（新增推广区 + 团队区）

<br>

- [ ] **Step 1: 在 `src/api/queries/distribution.ts` 新增团队查询**

在文件底部追加导出：

```typescript
export interface TeamSummary {
    directTeamSize: number;
    indirectTeamSize: number;
    totalTeamSize: number;
    orderCount: number;
    orderAmount: number;
    teamCommission: number;
}

export async function getMyTeamSummary(): Promise<TeamSummary> {
    const client = getGraphQLClient();
    const res: any = await client.request(`query MyTeamSummary {
        myTeamSummary { directTeamSize indirectTeamSize totalTeamSize orderCount orderAmount teamCommission }
    }`);
    return res.myTeamSummary;
}
```

- [ ] **Step 2: 在 `distribution.vue` 新增推广区与团队区（模板 + 逻辑）**

在「已申请」`<block v-else>` 内、佣金明细之前插入两个区块；脚本 `onShow` 里并行拉取 `myTeamSummary`：

模板（简化为结构示意，样式与现有 `dc-*` 类对齐）：

```vue
<!-- 推广区 -->
<view class="dc-promo">
  <text class="dc-promo__title">推广工具</text>
  <view class="dc-promo__link" @click="copy(promoLink(), '推广链接已复制')">
    <text>分享链接（好友下单你得佣金）</text>
    <text class="dc-promo__copy">复制</text>
  </view>
  <button class="dc-promo__poster" @click="showPoster = true">生成推广海报</button>
</view>

<!-- 团队/效果区 -->
<view class="dc-team" v-if="teamSummary">
  <text class="dc-team__title">我的团队</text>
  <view class="dc-team__grid">
    <view class="dc-team__cell"><text>直推</text><text>{{ teamSummary.directTeamSize }}人</text></view>
    <view class="dc-team__cell"><text>间推</text><text>{{ teamSummary.indirectTeamSize }}人</text></view>
    <view class="dc-team__cell"><text>带来订单</text><text>{{ teamSummary.orderCount }}单</text></view>
    <view class="dc-team__cell"><text>团队佣金</text><text>¥{{ formatYuan(teamSummary.teamCommission) }}</text></view>
  </view>
</view>
```

脚本新增：

```typescript
const tenantStore = useTenantStore();               // 若未 import
const teamSummary = ref<TeamSummary | null>(null);
const showPoster = ref(false);
const tenantName = computed(() => tenantStore.tenantName || '商城');

function promoLink(): string {
    const code = profile.value?.referralCode || '';
    if (typeof window !== 'undefined' && window.location) {
        const base = window.location.origin + '/#/pages/landing/invite';
        return code ? `${base}?ref=${encodeURIComponent(code)}` : base;
    }
    return code;
}

async function loadTeam() {
    try { teamSummary.value = await getMyTeamSummary(); } catch (e) { teamSummary.value = null; }
}
// 本文件生命周期用 onShow（非 onMounted）：在现有 onShow 内、并行拉取处补一句 await loadTeam();
```

海报弹窗：新增 `<dist-poster v-if="showPoster" :channel-name="tenantName" :inviter-name="profile.referralCode" :invite-code="profile.referralCode" />`；`copy` 函数复用现有 `dc-hero__code` 复制的实现。

> 导入补齐：
> - 在现有 `import { ... } from '../../api/queries/distribution'` 中追加 `getMyTeamSummary`、`TeamSummary`（`formatYuan` 已导入，无需再加）。
> - 新增 `import DistPoster from '../../components/dist-poster/dist-poster.vue';`
> - 新增 `import { useTenantStore } from '../../stores/tenant';`，顶部 `const tenantStore = useTenantStore();`。
> - `computed` 已由 script 顶部 `import { ref, computed } from 'vue';` 提供。
> - 生命期为 `onShow`（脚本顶部已 `import { onShow } from '@dcloudio/uni-app'`），把 `await loadTeam()` 追加进现有 `onShow` 回调（与 `loadCommissions()` 并列）。

- [ ] **Step 3: 前端 typecheck**

Run: `cd d:\zhao\vshop && npx vue-tsc --noEmit`
Expected: 无新增报错（仅校验）。

- [ ] **Step 4: Commit（vshop 仓库）**

```bash
cd d:\zhao\vshop && git add src/api/queries/distribution.ts src/pkg-user/pages/distribution.vue
git commit -m "feat(distribution): promo link/poster & team module in distribution center"
```

---

## 任务 6：集成实测 + 提交推送

<br>

- [ ] **Step 1: 后端 e2e 全绿**

Run: `cd d:\zhao\vendure && node tools/e2e-distribution.mjs`
Expected: 阶段40 断言 + 阶段41 团队聚合断言全部通过，退出码 0。

- [ ] **Step 2: 前端类型检查**

Run: `cd d:\zhao\vshop && npx vue-tsc --noEmit`（可结合 dev 后手工过一遍落地页绑定）

- [ ] **Step 3: 提交推送（vendure）**

```bash
cd d:\zhao\vendure && git add packages/distribution-plugin/src packages/distribution-plugin/lib tools/e2e-distribution.mjs && git commit -m "feat(distribution): phase41 promo landing & team summary" && git push
```

- [ ] **Step 4: 提交推送（vshop）**

```bash
cd d:\zhao\vshop && git add src doc/2026-08-22-分销推广落地-阶段41实施计划.md && git commit -m "feat(distribution): phase41 invite landing + promo poster + team view" && git push
```

---

## 非目标（本期不做）

- strapi 广告位/广告内容/多媒体发布中心复用（与商城无数据衔接）。
- 推广点击/访客埋点（YAGNI，团队效果从 CommissionRecord 聚合，零新表）。
- 分销员邀请人头像反查（落地页先用租户名/首字；后续可加 `findDistributorByReferralCode` 附带昵称）。
- 微信小程序落地页 `scene` 解析（本期 H5 为主，小程序复用现有卡片分享）。