# 会员权益整合中心（阶段36）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 vshop C 端搭建「会员权益整合中心」，聚合展示会员等级/成长值、积分、余额、优惠券、签到等权益；并在结算页打通「积分 + 余额」混合抵扣。

**Architecture:** 后端（member-level-plugin / checkin-plugin / recharge-card-plugin）C 端 GraphQL 接口已齐备（`myMemberInfo`/`myPointsHistory`/`redeemPoints`/`myRechargeBalance`/`checkinToday`/`checkin`），**无需新增后端 Query**。C 端新增 `api/queries/member.ts` 封装 + 会员中心聚合页 + 积分明细页 + profile 菜单入口；结算页结接线 `redeemPoints` 实现积分抵现后余额补差。

**Tech Stack:** Vue 3 `<script setup>`、uni-app（H5 + MP-WEIXIN）、Pinia、GraphQL（graphql-request）、SCSS、usePagination。

**前置状态：** 积分（阶段15）、等级（阶段30）、签到（阶段31）、优惠券（阶段12）、余额钱包（阶段33-35）均已就绪，且各自在 C 端有独立页面或 API 封装。

---

## 文件结构

- Create: `src/api/queries/member.ts`（权益中心聚合 API 封装）
- Create: `src/pkg-user/pages/member-center.vue`（会员权益中心聚合页）
- Create: `src/pkg-user/pages/points-history.vue`（积分明细页）
- Modify: `src/pkg-order/pages/checkout.vue`（积分抵扣 + 余额补差）
- Modify: `src/pkg-user/pages/profile.vue`（菜单入口）
- Modify: `src/pages.json`（注册新页 + 积分明细下拉刷新）

---

### Task 1: 权益中心 API 封装

**Files:**
- Create: `src/api/queries/member.ts`

- [ ] **Step 1: 创建 `member.ts`**

```ts
import { getGraphQLClient } from '../../utils/graphql';

const client = () => getGraphQLClient();

/** 我的会员信息（等级/成长值/积分/权益倍率） */
export async function getMyMemberInfo() {
    return client().request(`query MyMemberInfo {
        myMemberInfo { customerId level levelName growthValue points nextLevelThreshold nextLevelName pointsMultiplier redeemDiscountRate redeemCapRatio specialDiscountRate }
    }`);
}

/** 我的积分明细（分页） */
export async function getMyPointsHistory(options: { take: number; skip: number }) {
    return client().request(
        `query MyPointsHistory($skip: Int, $take: Int) { myPointsHistory(options: { skip: $skip, take: $take }) { items { id type amount balanceBefore balanceAfter orderId remark expiresAt createdAt } totalItems } }`,
        { skip: options.skip, take: options.take },
    );
}

/** 积分抵现：绑定即扣，返回重算价后的订单 */
export async function redeemPoints(points: number) {
    return client().request(`mutation RedeemPoints($points: Int!) { redeemPoints(points: $points) { id totalWithTax customFields }}`, { points });
}
```

- [ ] **Step 2: 优先复用已有封装（余额/优惠券/签到）**

余额复用 `src/api/mutations/recharge.ts` 的 `getMyBalance`（返回 `myRechargeBalance`）；优惠券复用 `src/api/queries/coupon.ts` 的 `getMyCoupons`。签到在 `member.ts` 追加：

```ts
/** 今日签到状态 */
export async function getCheckinToday() {
    return client().request(`query { checkinToday { checkedIn streak canCheckin } }`);
}
/** 签到 */
export async function doCheckin() {
    return client().request(`mutation { checkin { success reason points growth streak } }`);
}
```

- [ ] **Step 3: 类型检查通过**

Run: `cd d:\zhao\vshop && npx tsc --noEmit`
Expected: 无新增错误（`getGraphQLClient` 路径需按项目实际确认）。

- [ ] **Step 4: 提交**

```bash
cd d:\zhao\vshop
git add src/api/queries/member.ts
git commit -m "feat(vshop): 会员权益中心/积分/签到 API 封装"
```

---

### Task 2: 会员权益中心聚合页 member-center.vue

**Files:**
- Create: `src/pkg-user/pages/member-center.vue`

- [ ] **Step 1: 模板：等级头部 + 权益卡片 + 快捷入口**

````vue
<template>
  <view class="member-page">
    <!-- 等级头部 -->
    <view class="member-page__header">
      <text class="member-page__level">{{ info.levelName || ('Lv.' + info.level) || '普通会员' }}</text>
      <text class="member-page__growth">成长值 {{ info.growthValue }}{{ info.nextLevelThreshold ? ' / ' + info.nextLevelThreshold : '' }}</text>
      <view class="member-page__bar"><view class="member-page__bar--fill" :style="{ width: growthPercent + '%' }" /></view>
      <text v-if="info.nextLevelName" class="member-page__next">距离 {{ info.nextLevelName }} 还差 {{ nextThresholdDiff }}</text>
    </view>

    <!-- 权益卡片 -->
    <view class="member-page__cards">
      <view class="card" @click="navTo('/pkg-user/pages/points-history')">
        <text class="card__num">{{ info.points }}</text>
        <text class="card__label">我的积分</text>
      </view>
      <view class="card" @click="navTo('/pkg-user/pages/recharge')">
        <text class="card__num">¥{{ balanceYuan }}</text>
        <text class="card__label">余额</text>
      </view>
      <view class="card" @click="navTo('/pkg-promotion/pages/coupons')">
        <text class="card__num">{{ couponCount }}</text>
        <text class="card__label">优惠券</text>
      </view>
      <view class="card" @click="doCheckin">
        <text class="card__num" :class="{ 'card__num--done': checkedIn }">{{ checkedIn ? '已签' : '签到' }}</text>
        <text class="card__label">连签 {{ streak }} 天</text>
      </view>
    </view>

    <!-- 快捷入口 -->
    <view class="member-page__menu">
      <view class="menu-item" v-for="m in quickMenus" :key="m.url" @click="navTo(m.url)"><text>{{ m.label }}</text><text>></text></view>
    </view>
  </view>
</template>
````

- [ ] **Step 2: 脚本：并行聚合查询 + 扣款后刷新**

````ts
import { ref, computed, onMounted } from 'vue';
import { getMyMemberInfo, getCheckinToday, doCheckin } from '../../api/queries/member';
import { getMyBalance } from '../../api/mutations/recharge';
import { getMyCoupons } from '../../api/queries/coupon';
import { useUIStore } from '../../stores/ui';

const ui = useUIStore();
const info = ref<any>({});
const balance = ref(0);
const myCoupons = ref<any[]>([]);
const checkedIn = ref(false);
const streak = ref(0);

const balanceYuan = computed(() => (balance.value / 100).toFixed(2));
const couponCount = computed(() => (myCoupons.value.filter((c: any) => (c.status || '').toUpperCase() === 'UNUSED')).length);
const growthPercent = computed(() => {
    const t = info.value.nextLevelThreshold || 0;
    if (t <= 0) return 100;
    return Math.min(100, Math.round((info.value.growthValue / t) * 100));
});
const nextThresholdDiff = computed(() => Math.max(0, (info.value.nextLevelThreshold || 0) - (info.value.growthValue || 0)));

async function refresh() {
    const [mi, bal, cp, ct]: any[] = await Promise.all([
        getMyMemberInfo(), getMyBalance(), getMyCoupons(), getCheckinToday(),
    ]);
    info.value = mi?.myMemberInfo || {};
    balance.value = bal?.myRechargeBalance || 0;
    myCoupons.value = cp?.myCoupons || [];
    checkedIn.value = ct?.checkinToday?.checkedIn || false;
    streak.value = ct?.checkinToday?.streak || 0;
}
async function doCheckin() {
    if (checkedIn.value) return;
    try {
        ui.showLoading();
        const res: any = await doCheckin();
        const r = res?.checkin;
        ui.hideLoading();
        if (r?.success) {
            ui.showToast(`签到成功 +${r.points ?? 0}积分`, 'success');
            await refresh();
        } else {
            ui.showToast(r?.reason || '今日已签到', 'error');
        }
    } catch (e: any) { ui.hideLoading(); ui.showToast(e.message || '签到失败', 'error'); }
}
onMounted(async () => {
    try { await refresh(); } catch (e) { console.warn('[member-center] load failed', e); }
});
function navTo(url: string) { uni.navigateTo({ url }); }
const quickMenus = [
    { label: '我的订单', url: '/pkg-order/pages/orders' },
    { label: '余额明细', url: '/pkg-user/pages/balance-history' },
    { label: '营销中心', url: '/pkg-promotion/pages/coupons' },
];
````

> 注：`getMyCoupons` 返回 `res.myCoupons`，需结合 coupons.vue 确认实际字段名，必要时用 `getActiveCustomer` 之外的优惠券接口。

- [ ] **Step 3: 样式（复用 profile 视觉，编剧汇总）**

````scss
.member-page { min-height: 100vh; background: $bg-color;
  &__header { background: $brand-color; color: #fff; padding: 50rpx 30rpx;
    & .member-page__level { font-size: 44rpx; font-weight: bold; display: block; }
    & .member-page__growth { font-size: 26rpx; opacity: .9; margin-top: 8rpx; display: block; }
    & .member-page__next { font-size: 22rpx; opacity: .8; margin-top: 8rpx; display: block; } }
  &__bar { height: 14rpx; background: rgba(255,255,255,.3); border-radius: 10rpx; margin-top: 20rpx; overflow: hidden;
    &--fill { height: 100%; background: #fff; border-radius: 10rpx; } }
  &__cards { display: flex; margin: 20rpx; background: #fff; border-radius: $radius-md; padding: 20rpx 0;
    & .card { flex: 1; text-align: center; &__num { display: block; font-size: 36rpx; font-weight: bold; color: $brand-color; &--done { color: #52c41a; } } &__label { display: block; font-size: 24rpx; color: #666; margin-top: 8rpx; } } }
  &__menu { background: #fff; margin: 20rpx; border-radius: $radius-md; } }
.menu-item { display: flex; justify-content: space-between; padding: 30rpx; border-bottom: 1rpx solid $border-color; font-size: 28rpx; }
````

- [ ] **Step 4: 在 pages.json 注册 + 类型检查 + 提交**

- 注册 `member-center` / `points-history` 到 `src/pages.json` 的 `pkg-user`。类型检查通过后提交。

---

### Task 3: 积分明细页 points-history.vue

**Files:**
- Create: `src/pkg-user/pages/points-history.vue`

- [ ] **Step 1: 复用余额明细页骨架，改为积分流水**

`usePagination` 拉 `getMyPointsHistory`，type → 文案（earn/spend/adjust/refund/expire），正负号，下拉刷新。模板/样式参照 `balance-history.vue`，pages.json 该项增 `"enablePullDownRefresh": true`。

- [ ] **Step 2: 提交**

---

### Task 4: 结算页「积分 + 余额」混合抵扣

**Files:**
- Modify: `src/pkg-order/pages/checkout.vue`

- [ ] **Step 1: 新增积分抵扣区块（支付方式下方）**

````vue
<view class="section" v-if="pointsRate > 0">
  <view class="section__row">
    <text class="section__title">积分抵扣</text>
    <text class="section__hint">可用 {{ myPoints }} 积分，当前抵扣 ¥{{ pointsDiscountYuan }}</text>
  </view>
  <view class="points-redeem">
    <input v-model="redeemPointsInput" type="digit" placeholder="输入要抵扣的积分" class="points-redeem__input"/>
    <text class="points-redeem__go" @click="applyPointsRedeem">{{ applyingPoints ? '抵扣中…' : '抵扣' }}</text>
  </view>
</view>
````

- [ ] **Step 2: 计算与提交流程**

````ts
const myPoints = ref(0);
const redeemPointsInput = ref('');
const applyingPoints = ref(false);
// 积分抵现折扣：redeemPoints 后订单 totalWithTax 下降，差额即抵扣金额
const pointsDiscountYuan = computed(() => {
    const base = baseTotal.value;      // redeemPoints 前快照（初始 totalWithTax）
    return Math.max(0, (base - (cart.order?.totalWithTax || 0)) / 100).toFixed(2);
});
async function applyPointsRedeem() {
    const pts = Math.floor(Number(redeemPointsInput.value));
    if (!pts || pts > myPoints.value) { ui.showToast('积分不足'); return; }
    if (applyingPoints.value) return;
    applyingPoints.value = true;
    try {
        ui.showLoading();
        await redeemPoints(pts);
        const orderRes: any = await getActiveOrder();
        if (orderRes.activeOrder) { cart.setOrder(orderRes.activeOrder); baseTotal.value = orderRes.activeOrder.totalWithTax; }
        ui.hideLoading(); ui.showToast('已抵扣', 'success');
    } catch (e: any) { ui.hideLoading(); ui.showToast(e.message); }
    applyingPoints.value = false;
}
````

- [ ] **Step 3: 余额预检随抵扣后金额联动**

`redeemPoints` 重算价使 `cart.order.totalWithTax` 下降 → `balancesInsufficient`（`payTotal > balance`）自动跟随减免后的应付，无需额外改动；但需在 `onMounted` 记录 `baseTotal` 用于展示抵扣额。

- [ ] **Step 4: 类型检查 + 提交**

---

### Task 5: profile 菜单入口

**Files:**
- Modify: `src/pkg-user/pages/profile.vue`

- [ ] **Step 1: 在菜单顶部加「会员中心」「积分明细」**

````vue
<view class="menu-item" @click="navTo('/pkg-user/pages/member-center')"><text>会员中心</text><text>></text></view>
<view class="menu-item" @click="navTo('/pkg-user/pages/points-history')"><text>积分明细</text><text>></text></view>
````

- [ ] **Step 2: 提交**

---

## 验收清单

- [x] 会员中心页展示等级/成长值进度、积分、余额、优惠券数、签到状态（member-center.vue）
- [x] 会员中心一键签到 → +积分、连签天数刷新
- [x] 积分明细页正确展示流水（earn/spend/adjust/expire、正负、分页、下拉刷新）
- [x] 结算页积分抵扣 → 订单价下降 → 余额支付补差 → Settled（积分抵扣金额累计展示）
- [x] 余额不足预检随积分抵扣后金额联动（cart.totalWithTax 更新自动触发）
- [x] profile「会员中心/积分明细」入口可达
- [ ] 积分+余额混合抵扣 e2e 实测（dev 环境充值→积分抵→余额补差→流水）待真人验证回填